path:
    train_path: ./NLP_dataset/train/train.csv
    dev_path: ./NLP_dataset/train/train.csv # 수정 필요
    test_path: ./NLP_dataset/train/train.csv # 수정 필요
    predict_path: ./NLP_dataset/test/test_data.csv

#data:
#    shuffle: True
#    augmentation: # adea, bt 등등
    
model:
    model_name: klue/roberta-small
    saved_name: base_model

train:
    loss_name : 'CE'
    gpus: 1
    batch_size: 16
    max_epoch: 20
    learning_rate: 5e-5
    logging_step: 1
    save_total_limit : 5 # number of total save model.
    save_steps : 500 # model saving step.
    warmup_steps : 500 # number of warmup steps for learning rate scheduler
    weight_decay : 0.01 # strength of weight decay
    logging_steps : 100  # log saving step.
    eval_steps : 500 # evaluation step